{
  "name": "skysend-public-site",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "python site/build.py",
    "validate": "python tools/validate_site.py",
    "preview": "python -m http.server 4173 --directory dist"
  }
}
